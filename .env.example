# OneMap API token — expires roughly every 3 days, see SETUP.md step 5
ONEMAP_TOKEN=

# Optional: your OneMap account login, only needed if you use
# src/refresh_onemap_token.py to fetch a new ONEMAP_TOKEN automatically
# instead of doing it by hand. Never used for anything else.
ONEMAP_EMAIL=
ONEMAP_PASSWORD=
